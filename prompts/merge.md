You are updating one person's CRM profile from new Signal messages. Two files are attached: the current profile (a `.md` file) and a file of new messages since the last refresh.

Merge ONLY genuinely new, factual information. Do not invent anything; use only what the messages actually support. Be concise.

PROVENANCE: every line in the new-messages file starts with a source id like `⟨m89123⟩`. Citation rules:
- Every bullet you add or update under `## Talking points` MUST end with the id(s) of the message(s) it came from, e.g. `- **2026-07-04** ask if she ordered the espresso machine ⟨m89123, m89150⟩`. Cite 1-3 ids per bullet — the load-bearing messages, not every message.
- Copy ids EXACTLY as they appear in the new-messages file. NEVER invent, guess, or alter an id. If you can't identify a source message, omit the citation rather than fabricate one.
- Do NOT add citations to `## What I know` or `## Open questions` — those sections stay clean prose.

- Update the bullets under `## What I know` where things have changed, or add new bullets for genuinely new facts. Don't restate what's already there unverbatim — integrate.
- Maintain the `## Talking points` section (create it immediately before `## Timeline` if it doesn't exist yet). This is a short, conversation-ready list — the things a good listener would remember to bring up: upcoming plans and dates, important things they recently mentioned (worries, wins, purchases, trips, family events), and follow-ups ("ask how X went"). Rules:
  - Format every bullet as `- **YYYY-MM-DD** text` — use the event's date for upcoming things, or the mention date for recent mentions. Undated bullets are allowed only when no date is knowable, and go last.
  - Keep it max ~8 bullets. Add new items from the messages, update ones that changed, and DELETE items that are now past, stale, or resolved — this section must stay current, not accumulate.
  - Prefer specific, actionable phrasings ("ask if she ordered the espresso machine") over vague ones ("likes coffee").
- Update `## Open questions`: add new open questions raised by the messages, and remove/resolve ones the messages answer.
- Update the metadata block at the top of the file:
  - Set `Last contact` to the latest date seen in the new messages.
  - Fill in `Relationship` and/or `Birthday` ONLY if newly and clearly learned from the messages (don't guess).
  - Leave every other metadata field exactly as-is.
- DO NOT touch the `## Timeline` section AT ALL, in any way — a separate compaction step owns that section exclusively. Leave it exactly as it currently appears, byte-for-byte.

Save your changes using the edit tool on the profile file only. Do not create or delete any other files. Do not message anyone or take any action outside editing this one profile file.
